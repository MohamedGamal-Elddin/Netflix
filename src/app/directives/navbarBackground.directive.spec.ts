/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NavbarBackgroundDirective } from './navbarBackground.directive';

describe('Directive: NavbarBackground', () => {
  it('should create an instance', () => {
    const directive = new NavbarBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
