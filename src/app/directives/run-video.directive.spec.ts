import { RunVideoDirective } from './run-video.directive';

describe('RunVideoDirective', () => {
  it('should create an instance', () => {
    const directive = new RunVideoDirective();
    expect(directive).toBeTruthy();
  });
});
